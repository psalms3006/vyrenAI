# ==========================================================
# VYREN OS - Requirements
# ==========================================================

# === CORE ===
pyyaml>=6.0
psutil>=5.9.0
google-genai>=1.0.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0
websockets>=12.0

# === VOICE (REQUIRED — VYREN is voice-first) ===
# sounddevice/numpy back the Gemini Live mic+speaker streams (voice_engine/engine.py).
sounddevice>=0.4.6
numpy>=1.24.0

# Local TTS — used ONLY when GEMINI_API_KEY is absent (voice/runtime.py fallback).
# Not used while Gemini Live native audio is active.
pyttsx3>=2.90

# Local STT for the offline conversation loop (voice/offline_loop.py) — used
# ONLY when GEMINI_API_KEY is absent or Gemini Live gives up reconnecting.
# CPU-only, int8 quantized; loads lazily on first offline utterance.
faster-whisper>=1.0.0

# Keyboard listener (for text-mode hotkey, not for voice)
pynput>=1.7.6

# === SCREEN CAPTURE (OPTIONAL) ===
# tools/screen_tools.py lazily imports these at call time; VYREN runs fine
# without them, screen-capture tools just report "unavailable" until installed.
Pillow>=11
mss>=10

# ==========================================================
# NOT YET USED — no code in this repo imports these.
# Left here (commented) as a placeholder for planned subsystems
# (Vision/CV, OCR, Desktop Automation, Semantic Memory, Offline
# Knowledge) referenced in the Dependency Manager spec. Uncomment
# a line only once the corresponding subsystem is actually built —
# several of these (easyocr, faiss-cpu, sentence-transformers) are
# large/slow installs with no payoff until then.
# ==========================================================

# Vision
# opencv-python>=4.10

# OCR
# easyocr>=1.7.2

# Desktop Automation
# pyautogui>=0.9.54
# pygetwindow>=0.0.9
# pyperclip>=1.9
# watchdog>=5

# AI / Semantic Memory
# sentence-transformers>=3
# faiss-cpu

# Offline Knowledge
# beautifulsoup4>=4.12
# lxml>=5.2

# Misc utilities (not currently imported anywhere)
# scipy>=1.15
# soundfile>=0.13
# requests>=2.32
# tqdm>=4.67
# packaging>=24
